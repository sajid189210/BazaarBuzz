<% blocks.head=` <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>` %><div
        class="mx-auto max-w-[1100px] px-4 sm:px-6 lg:px-8 pt-6 pb-16">
        <nav class="hidden lg:flex items-center gap-2 text-sm text-gray-500 mb-8"> <a href="/"
                class="hover:text-[var(--accent)] transition-colors btn-press">Home</a> <i class="fa-solid fa-chevron-right text-xs text-gray-400"></i> <span class="text-gray-800 font-medium">My Orders</span> </nav>
        <h1 class="text-2xl lg:text-3xl font-bold text-gray-900 mb-8">My Orders</h1>
        <% if(orders && orders.length> 0) { %> <div class="space-y-4">
                <% orders.forEach(order=> { %> <div
                        class="rounded-xl border border-gray-200 bg-white p-6 hover:border-gray-300 transition-colors">
                        <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-3">
                            <div>
                                <p class="text-xs text-gray-400">Order ID</p>
                                <p class="text-sm font-medium text-gray-900 capitalize">
                                    <%= String(order._id).slice(-8) %>
                                </p>
                            </div>
                            <div class="text-left sm:text-right">
                                <p class="text-xs text-gray-400">Placed on</p>
                                <p class="text-sm text-gray-700">
                                    <%= new Date(order.createdAt).toLocaleDateString('en-IN', { day: 'numeric' ,
                                        month: 'short' , year: 'numeric' }) %>
                                </p>
                            </div>
                            <div class="text-left sm:text-right">
                                <p class="text-xs text-gray-400">Status</p> <span
                                    class="inline-block text-xs font-semibold px-2.5 py-0.5 rounded-full                         <%= order.status === 'delivered' ? 'bg-green-50 text-green-700' :                            order.status === 'cancelled' ? 'bg-red-50 text-red-700' :                            order.status === 'payment_failed' ? 'bg-red-50 text-red-700' :                            'bg-amber-50 text-amber-700' %>">
                                    <%= order.status %>
                                </span>
                            </div>
                            <div class="text-left sm:text-right">
                                <p class="text-xs text-gray-400">Total</p>
                                <p class="text-sm font-semibold text-gray-900">₹<%= order.total.toLocaleString() %>
                                </p>
                            </div> <a href="/user/checkout/orderSummary/<%= order._id %>"
                                class="shrink-0 rounded-lg border border-gray-300 px-4 py-2 text-xs font-semibold text-gray-700 hover:border-gray-400 transition-colors text-center">View
                                Details</a>
                        </div>
                    </div>
                    <% }) %>
            </div>
            <% } else { %>
                <div class="flex flex-col items-center justify-center py-20"> <i class="fa-regular fa-rectangle-list text-5xl text-gray-300 mb-4"></i>
                    <h3 class="text-lg font-semibold text-gray-500">No orders yet</h3> <a href="/user/productList"
                        class="mt-4 text-sm font-medium text-[var(--accent)] hover:text-[var(--accent)] transition-colors btn-press">Start
                        Shopping</a>
                </div>
                <% } %>
    </div>